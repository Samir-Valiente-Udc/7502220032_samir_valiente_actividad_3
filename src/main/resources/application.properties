# Configuración del servidor
server.port=8080

# Configuración de la fuente de datos (DataSource) para MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/DB_EJERCICIO_16?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=samir_valiente
spring.datasource.password=AbcdeUdeC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA (Java Persistence API)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración de Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML

logging.level.org.springframework.security=DEBUG
